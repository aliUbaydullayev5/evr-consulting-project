import React from 'react'
import Container from "@/root/style";

const Root = ({children}) => {
    return(
        <Container>
            {children}
        </Container>
    )
}

export default Root